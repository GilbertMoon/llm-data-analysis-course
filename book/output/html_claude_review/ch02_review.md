# ch02 강의안 검토 리포트

> **대상 독자**: 파이썬 기초 경험이 있는 비전공자, 데이터 분석 기본 개념 보유 학생  
> **검토 기준**: 한 학기 강의 교재로서 독립적 학습 가능 여부  
> **작성일**: 2026-06-19  
> **검토 파일**: `book/chapters/ch02_environment_setup.md`

---

## 검토 지침 (Codex Prompt Format)

아래 각 항목은 `[섹션명]` 위치를 기준으로 문제를 설명하고, 구체적인 수정/보완 방향을 제시합니다.  
**[필수 수정]** = 학습에 직접적 혼란을 야기하는 항목  
**[보완 권장]** = 추가 시 학습 효과가 크게 향상되는 항목

---

## 1. 필수 수정 항목

---

### [1-1] git 설치 확인 없이 `git clone` 명령어 등장 — [섹션 5.3]

**문제**  
섹션 5.3에서 `git clone https://github.com/본인아이디/...` 명령을 안내하지만, git이 설치되어 있지 않은 학생은 이 명령이 실행되지 않는다. 비전공자는 git이 기본 설치되어 있지 않은 경우가 많다. git 설치 여부 확인이나 설치 방법에 대한 안내가 전혀 없다.

**수정 지시**  
섹션 5.3 앞 또는 섹션 3.1 내에 다음 내용을 추가한다:

```
Git 설치 확인

터미널에서 다음 명령으로 git이 설치되어 있는지 확인합니다.

git --version

출력 예: git version 2.43.0.windows.1

명령이 실행되지 않으면 git이 설치되어 있지 않은 것입니다.
git 공식 사이트(https://git-scm.com)에서 설치 파일을 내려받아 설치합니다.
설치 후 VS Code나 PowerShell을 다시 시작해야 명령이 인식됩니다.
```

---

### [1-2] GitHub 계정 생성 안내 없음 — [섹션 5.3]

**문제**  
섹션 5.3과 학습 목표에 "본인 GitHub 계정에 개인 실습 저장소를 만든다"고 명시되어 있지만, GitHub 계정이 없는 학생을 위한 계정 생성 안내가 없다. 강의 대상이 비전공자임을 감안하면 GitHub 자체가 처음인 학생이 있을 수 있다.

**수정 지시**  
섹션 5.3 앞에 다음 내용을 추가한다:

```
GitHub 계정 준비

GitHub는 코드와 프로젝트를 저장하고 관리하는 온라인 플랫폼입니다.
계정이 없다면 https://github.com 에 접속해 무료 계정을 만듭니다.

계정 생성 시 주의사항:
- 이메일 인증을 완료해야 저장소를 만들 수 있습니다.
- 아이디(username)는 이후 저장소 URL에 포함되므로 기억하기 쉬운 이름으로 만듭니다.
- 학교 이메일로 가입하면 GitHub Student 혜택(Pro 플랜 무료)을 신청할 수 있습니다.
```

---

### [1-3] Jupyter Notebook에서 `.venv` 커널 선택 안내 없음 — [섹션 5, 섹션 7]

**문제**  
`jupyter notebook`을 실행한 뒤 Notebook 파일을 열면 Python 커널을 선택해야 한다. 가상환경(`.venv`)이 커널 목록에 자동으로 나타나지 않는 경우가 있으며, 이때 잘못된 Python 환경(시스템 기본 Python)으로 실행해 패키지를 찾지 못하는 오류가 발생할 수 있다. 이에 대한 안내가 전혀 없다.

**수정 지시**  
섹션 5.1 또는 섹션 7 앞에 다음 내용을 추가한다:

```
Jupyter Notebook 커널 확인

jupyter notebook을 실행하고 Notebook 파일을 열면 오른쪽 상단에 커널 이름이 표시됩니다.
커널은 코드를 실제로 실행하는 Python 환경입니다.

.venv 가상환경이 커널 목록에 보이지 않는다면 다음 명령으로 등록합니다:

# 가상환경이 활성화된 상태에서 실행
pip install ipykernel
python -m ipykernel install --user --name=.venv --display-name "Python (.venv)"

등록 후 jupyter notebook을 다시 실행하면 "Python (.venv)" 커널을 선택할 수 있습니다.
커널이 .venv인지 확인한 뒤 실습을 진행합니다.
```

---

### [1-4] `display()` 함수 미설명 — [섹션 5.6, 섹션 7.4]

**문제**  
섹션 5.6과 7.4에서 `display(customers.head())`를 사용하지만, `display()`는 Jupyter Notebook에서만 작동하는 함수다. 일반 Python 파일(.py)이나 일부 터미널 환경에서는 `NameError: name 'display' is not defined` 오류가 발생한다. 이 차이를 모르는 학생은 오류의 원인을 찾지 못한다.

**수정 지시**  
`display()` 가 처음 등장하는 섹션 5.6 앞에 다음 설명을 추가한다:

```
display()는 Jupyter Notebook에서 표나 그래프를 더 보기 좋게 출력하는 함수입니다.
Jupyter Notebook에서는 자동으로 사용 가능하며, 별도의 import 없이 쓸 수 있습니다.

display(customers.head())   # Jupyter Notebook 전용
print(customers.head())     # 모든 Python 환경에서 사용 가능

Jupyter Notebook이 아닌 환경(.py 파일, 터미널 등)에서 display()를 쓰면
"NameError: name 'display' is not defined" 오류가 발생합니다.
이 경우 print()로 대체합니다.
```

---

### [1-5] `requirements.txt` 내용 미공개 — [섹션 3.8, 섹션 5.1]

**문제**  
`pip install -r requirements.txt`를 실행하라고 안내하지만, 파일 안에 어떤 패키지가 있는지 학생이 확인하는 방법을 알려주지 않는다. 일부 학생은 자신도 모르는 패키지가 설치되는 것에 불안감을 느낄 수 있고, 패키지 목록을 사전에 확인하는 습관을 기르는 것도 중요하다.

**수정 지시**  
섹션 5.1 명령어 표 앞에 다음 내용을 추가한다:

```
설치 전 패키지 목록 확인

pip install 명령을 실행하기 전에 requirements.txt 내용을 먼저 확인하는 습관이 좋습니다.

# Windows PowerShell
type requirements.txt

# macOS/Linux
cat requirements.txt

파일에 어떤 패키지와 버전이 포함되어 있는지 확인한 뒤 설치를 진행합니다.
```

---

### [1-6] `.env.example` → `.env` 파일 생성 절차 없음 — [섹션 3.9]

**문제**  
섹션 3.9에서 `.env.example` 파일이 언급되지만, 이것을 실제 `.env` 파일로 어떻게 만드는지 절차가 없다. 비전공자는 `.env.example`을 복사해 이름을 바꾸고 내용을 수정하는 작업을 모를 수 있다.

**수정 지시**  
섹션 3.9 또는 섹션 5 내 `.env` 관련 설명에 다음 절차를 추가한다:

```
.env 파일 만드는 방법

1. 프로젝트 폴더에 있는 .env.example 파일을 복사합니다.

# Windows PowerShell
Copy-Item .env.example .env

# macOS/Linux
cp .env.example .env

2. VS Code에서 .env 파일을 열고 실제 API Key를 입력합니다.

GEMINI_API_KEY=여기에_실제_API_Key_입력

3. 저장합니다. .env 파일은 .gitignore에 포함되어 있으므로
   git add . 를 해도 GitHub에 올라가지 않습니다.

⚠️ .env 파일을 GitHub에 올리면 API Key가 공개됩니다. 절대 주의합니다.
```

---

### [1-7] 가상환경 비활성화 방법 미안내 — [섹션 5.1]

**문제**  
가상환경 활성화 방법은 있지만 비활성화(`deactivate`) 방법이 없다. 학생이 다른 작업으로 넘어갈 때, 또는 가상환경을 잘못 활성화했을 때 어떻게 종료하는지 알 수 없다.

**수정 지시**  
섹션 5.1 명령어 표에 다음 항목을 추가한다:

| 명령어 | 의미 |
|--------|------|
| `deactivate` | 현재 활성화된 가상환경 비활성화 |

또한 가상환경 섹션 아래에 다음 설명을 추가한다:

```
가상환경 비활성화

가상환경에서 벗어나려면 다음 명령을 실행합니다.

deactivate

프롬프트에서 (.venv) 표시가 사라지면 비활성화된 것입니다.
```

---

### [1-8] `code .` 명령 실패 원인 미설명 — [섹션 5.4]

**문제**  
`code .` 명령이 실행되지 않을 때 "VS Code 메뉴에서 직접 열어도 된다"고만 안내하고, 왜 실패하는지 설명이 없다. `code` 명령은 VS Code 설치 시 PATH 설정을 선택했을 때만 사용 가능하며, 이를 모르는 학생은 자신이 무언가 잘못한 것으로 오해할 수 있다.

**수정 지시**  
섹션 5.4의 `code .` 안내 아래에 다음 내용을 추가한다:

```
code . 명령이 실행되지 않는 경우

이 명령은 VS Code 설치 시 "Add to PATH" 옵션을 선택했을 때만 작동합니다.
명령이 실행되지 않아도 실습에는 문제가 없습니다.

대신 다음 방법으로 폴더를 엽니다:
- VS Code 실행 → File → Open Folder → 개인 저장소 폴더 선택

PATH 설정을 추가하고 싶다면 VS Code Command Palette(Ctrl+Shift+P)에서
"Shell Command: Install 'code' command in PATH"를 실행합니다.
그 다음 터미널을 다시 열면 code . 명령이 사용 가능합니다.
```

---

### [1-9] 수업 시간 구성표 합계와 본문 설명 불일치 — [수업 시간 구성 표]

**문제**  
수업 시간 구성 표의 권장 시간 합계: 30+30+30+50+50+30 = 220분 = 3시간 40분.  
그런데 본문에 "기본 수업은 약 3시간을 기준으로 구성되어 있습니다"라고 적혀 있다. 40분 차이가 설명 없이 존재한다. ch01 리뷰와 동일한 구조적 문제.

**수정 지시**  
다음 두 방법 중 하나를 선택해 수정한다:

- 방법 A: 표의 일부 항목 시간을 줄여 합계를 180분(3시간)으로 맞춘다.
- 방법 B: 본문을 "기본 수업은 약 3시간 30분을 기준으로 구성되어 있습니다"로 수정한다.

또한 ch01과 동일하게 각 구성 항목에 해당 섹션 번호를 매핑해 추가한다.

---

### [1-10] `scripts/generate_sample_data.py` 경로 표기 혼재 — [섹션 5.1, 섹션 5.5]

**문제**  
섹션 5.1은 "Windows PowerShell 기준"이라고 명시했음에도 `python scripts/generate_sample_data.py`로 슬래시(`/`)를 사용하고 있다. PowerShell에서는 백슬래시(`\`)가 표준이며, 이 혼재가 학생에게 혼란을 줄 수 있다. ch01과 동일한 문제.

**수정 지시**  
섹션 5.1의 PowerShell 명령어 블록을 다음과 같이 수정한다:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
python scripts\generate_sample_data.py
jupyter notebook
```

명령어 표도 동일하게 수정한다.  
섹션 5.2(macOS/Linux)는 슬래시를 유지한다.

---

### [1-11] PowerShell 실행 정책 오류 해결책에 `cmd` 대안 없음 — [섹션 5.1]

**문제**  
PowerShell 실행 정책 오류(`running scripts is disabled`) 해결책으로 `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`만 제시하고 있다. 학교나 기업 PC에서는 이 명령도 막혀 있을 수 있다. 이 경우 명령 프롬프트(cmd)를 사용하는 대안이 없다.

**수정 지시**  
PowerShell 실행 정책 오류 섹션에 cmd 대안을 추가한다:

```
방법 2: 명령 프롬프트(cmd) 사용

PowerShell 대신 Windows 명령 프롬프트(cmd)를 사용하면 실행 정책 문제가 발생하지 않습니다.
시작 메뉴에서 "명령 프롬프트"를 검색해 실행합니다.

cmd에서 가상환경 활성화 명령어는 다음과 같습니다:
.venv\Scripts\activate.bat

방법 3: VS Code 내장 터미널 사용

VS Code 메뉴 → Terminal → New Terminal 에서
터미널 종류를 "Command Prompt"로 선택해도 됩니다.
```

---

### [1-12] commit/push 실습 절차 없음 — [섹션 4, 섹션 9]

**문제**  
학습 목표에 "실습 결과를 commit/push할 수 있는 GitHub 작업 흐름"이 결과물로 포함되어 있고, 실습 흐름 마지막 단계(13번)에도 "실습 결과를 본인 저장소에 commit/push"가 있다. 그런데 실습 코드 섹션(5)에는 git 명령어 관련 내용이 전혀 없다.

**수정 지시**  
섹션 5 마지막 또는 섹션 5.4 이후에 git 기본 작업 흐름 섹션을 추가한다:

```markdown
### 5.7 실습 결과 GitHub에 저장하기

실습을 진행하면서 변경한 파일을 본인 GitHub 저장소에 저장합니다.
VS Code의 소스 제어 패널(Ctrl+Shift+G)을 사용하거나, 아래 명령어를 사용합니다.

# 변경된 파일 상태 확인
git status

# 변경 파일을 스테이징 영역에 추가
git add .

# 변경 내용을 커밋(저장)
git commit -m "2장 환경 설정 실습 완료"

# GitHub 원격 저장소에 업로드
git push origin main

커밋 메시지는 어떤 작업을 했는지 간단히 적는 메모입니다.
push가 완료되면 GitHub 저장소 웹 페이지에서 변경 내용을 확인할 수 있습니다.
```

---

## 2. 보완 권장 항목

---

### [2-1] GitHub 핵심 용어 설명 부재 — [섹션 3.4, 섹션 5.3]

**문제**  
"commit", "push", "clone", "repository(저장소)", "branch"가 설명 없이 등장한다. 비전공자에게는 생소한 용어들이다.

**보완 지시**  
섹션 3.4 앞 또는 섹션 5.3 앞에 GitHub 핵심 용어 표를 추가한다:

| 용어 | 의미 |
|------|------|
| Repository(저장소) | 프로젝트 파일과 변경 이력을 저장하는 공간 |
| Clone | 원격 저장소를 내 PC로 내려받는 것 |
| Commit | 변경 내용을 저장소에 기록하는 것 (로컬) |
| Push | 로컬 commit을 원격 저장소(GitHub)에 업로드하는 것 |
| Pull | 원격 저장소의 최신 내용을 내 PC로 가져오는 것 |

---

### [2-2] VS Code Python 확장 설치 안내 없음 — [섹션 3.3]

**문제**  
VS Code를 처음 설치한 학생은 Python 관련 기능(자동완성, 가상환경 인식 등)을 쓰려면 Python 확장을 설치해야 한다. 이 안내가 없으면 가상환경이 VS Code에서 인식되지 않거나, 코드 실행이 안 되는 문제가 생긴다.

**보완 지시**  
섹션 3.3 또는 섹션 5.4 뒤에 다음 내용을 추가한다:

```
VS Code 필수 확장 설치

VS Code 왼쪽 사이드바의 확장(Extensions, Ctrl+Shift+X) 아이콘을 클릭합니다.
아래 확장을 검색해 설치합니다:

- Python (Microsoft 제공): Python 코드 실행, 가상환경 인식, 자동완성
- Jupyter (Microsoft 제공): VS Code 안에서 Jupyter Notebook 실행

이 두 확장은 수업 전 과정에서 계속 사용합니다.
```

---

### [2-3] `.gitignore` 파일 역할 및 내용 설명 없음 — [섹션 3.7, 섹션 3.9]

**문제**  
`.venv`, `.env`를 GitHub에 올리지 않는다고 여러 곳에 언급하지만, 실제로 어떤 설정이 이를 막는지(`.gitignore`) 설명이 없다. 학생이 실수로 `git add .`를 했을 때 왜 `.venv`가 포함되지 않는지 이해하지 못한다.

**보완 지시**  
섹션 3.7 또는 섹션 3.9 뒤에 다음 설명을 추가한다:

```
.gitignore 파일의 역할

.gitignore 파일에 등록된 파일과 폴더는 git add . 를 해도 
GitHub에 올라가지 않습니다.

이 프로젝트의 .gitignore에는 다음이 포함되어 있어야 합니다:

.venv/           # 가상환경 폴더 (용량이 크고 PC마다 다름)
.env             # API Key 등 민감 정보
__pycache__/     # Python 캐시 파일
.ipynb_checkpoints/  # Jupyter 임시 파일

.gitignore 파일은 프로젝트 루트 폴더에 있어야 합니다.
VS Code에서 열어 내용을 확인해 보세요.
```

---

### [2-4] `pip install --upgrade pip` 이유 미설명 — [섹션 5.1]

**문제**  
`python -m pip install --upgrade pip` 명령이 "pip 최신화"라고만 설명되어 있다. 왜 패키지 설치 전에 pip를 먼저 최신화해야 하는지 이유를 모르는 학생이 많다.

**보완 지시**  
해당 명령어 설명에 한 줄 추가한다:

```
pip은 Python 패키지를 설치하는 도구입니다. 버전이 오래되면 일부 패키지 설치가
실패할 수 있습니다. 패키지 설치 전에 pip을 최신화하는 것이 안전합니다.
```

---

### [2-5] 실습 흐름 14단계에서 '오류 발생 시' 항목이 마지막에 배치 — [섹션 4]

**문제**  
실습 흐름 14번 "오류가 발생하면 LLM을 활용해 원인 정리"가 마치 선택적인 마지막 단계처럼 보인다. 그러나 실제로는 어느 단계에서든 오류가 발생할 수 있으며, LLM 활용은 모든 단계에 걸친 도구다. 위치가 오해를 유발할 수 있다.

**보완 지시**  
14번 항목을 별도 박스(Callout)로 분리해 "어느 단계에서든 오류가 발생하면..." 형식으로 안내한다:

```
💡 오류 발생 시

위 실습 흐름 어느 단계에서든 오류가 발생할 수 있습니다.
오류가 나면 당황하지 말고 다음 순서로 해결합니다:

1. 오류 메시지를 읽고 키워드를 파악합니다 (예: FileNotFoundError, ModuleNotFoundError)
2. 현재 단계(몇 번 단계)에서 발생했는지 확인합니다
3. 오류 메시지에서 API Key, 비밀번호, 개인정보를 제거합니다
4. 정리된 오류 메시지를 LLM에게 질문합니다 (섹션 6.1 프롬프트 활용)
5. LLM이 제안한 명령어는 의미를 확인한 뒤 실행합니다
```

---

### [2-6] 섹션 5.6 환경 점검 코드에서 `from pathlib import Path` 중복 — [섹션 5.6]

**문제**  
섹션 5.6에서 `from pathlib import Path`와 `import sys`, `import pandas as pd`를 import하는 첫 번째 코드 블록이 있고, 그 바로 아래 `FileNotFoundError` 해결 코드 블록에서도 `from pathlib import Path`를 다시 import한다. Jupyter Notebook에서는 각 셀이 독립적으로 실행되므로 이해할 수 있지만, 초보자는 "왜 같은 것을 두 번 쓰는가" 혼란스러울 수 있다.

**보완 지시**  
두 번째 코드 블록 앞에 다음 설명을 추가한다:

```
아래 코드는 위 코드와 별개의 셀에서 실행하는 경우를 위한 것입니다.
위 셀에서 이미 Path를 import했다면 from pathlib import Path 줄은 생략해도 됩니다.
```

---

### [2-7] 연습 문제에 힌트 또는 채점 기준 없음 — [섹션 9]

**문제**  
ch01과 동일하게, 연습 문제가 제출 형식만 있고 채점 기준이나 힌트가 없다.  
특히 심화 과제 1번("오류 해결 보고서 작성")은 오류가 발생하지 않은 학생이 어떻게 작성해야 할지 모호하다.

**보완 지시**  
심화 과제 1번에 다음 안내를 추가한다:

```
오류가 발생하지 않았더라도 다음 중 하나를 선택해 작성하세요:
- 실습 중 가장 시간이 걸렸던 단계와 그 이유
- 가상환경 활성화 확인 방법과 확인 결과
- Jupyter Notebook 커널 선택 과정에서 겪은 점

보고서 예시 구조:
1. 발생 단계: (예: 5단계 - 패키지 설치)
2. 오류 메시지 요약: (실제 메시지 또는 상황 설명)
3. 원인 추정: (자신이 판단한 이유)
4. 해결 방법: (실제 실행한 방법)
5. 재발 방지: (다음에는 어떻게 할 것인가)
```

---

### [2-8] 강사 저장소 URL 또는 이름 미명시 — [섹션 3.4, 섹션 5.3]

**문제**  
"강사 GitHub 저장소에 접속합니다"라고 안내하지만, 실제 저장소 URL이나 이름이 없다. 학생이 어디에 접속해야 하는지 알 수 없다. 연습 문제 2번에도 "강사 GitHub 저장소에서 확인하세요"라고 하지만 URL이 없다.

**보완 지시**  
강사 저장소 URL 플레이스홀더를 추가한다:

```
강사 GitHub 저장소: https://github.com/[강사-계정]/llm-data-analysis-course

※ 강사 저장소 주소는 수업 오리엔테이션 자료 또는 LMS에서 확인합니다.
```

또는 교재 앞부분(1장 또는 2장 시작)에 수업 정보 박스를 만들어 저장소 URL을 기입하는 양식을 제공한다.

---

### [2-9] `python3` 명령 vs `python` 명령 — Windows에서의 혼란 가능성 — [섹션 5.1, 섹션 5.2]

**문제**  
섹션 5.1(Windows)에서는 `python --version`, 5.2(macOS/Linux)에서는 `python3 --version`을 사용한다. 그런데 일부 Windows 환경(특히 Windows App Store에서 설치한 Python)에서는 `python3`이 사용될 수 있다. 섹션 5.2 마지막에 "Python 명령어가 `python3`일 수 있습니다"라는 안내가 있지만 이것이 Windows에도 해당할 수 있음이 명시되지 않는다.

**보완 지시**  
섹션 5.1 `python --version` 안내 아래에 다음 내용을 추가한다:

```
일부 Windows 환경에서는 python 대신 python3을 사용해야 할 수 있습니다.
python --version이 실행되지 않으면 python3 --version을 시도합니다.
이후 실습에서도 동일하게 python을 python3으로 바꾸어 사용합니다.
```

---

### [2-10] 핵심 용어 정리 섹션 부재 — [전체 구조]

**문제**  
2장에 새로 등장하는 용어들(가상환경, venv, requirements.txt, .env, .gitignore, PATH, commit, push, clone, 커널, ipykernel)이 각 섹션에 분산되어 있다. ch01과 동일하게 복습용 용어 정리 섹션이 없다.

**보완 지시**  
섹션 10(정리) 이후 또는 정리 섹션 안에 "이 장에서 사용한 주요 용어" 표를 추가한다:

| 용어 | 설명 |
|------|------|
| 가상환경(venv) | 프로젝트별 독립 Python 실행 환경 |
| requirements.txt | 필요한 패키지 목록 파일 |
| .env | API Key 등 민감 환경변수 저장 파일 |
| .gitignore | GitHub에 올리지 않을 파일/폴더 목록 |
| PATH | 운영체제가 명령어를 찾는 경로 목록 |
| 커널 | Jupyter Notebook에서 코드를 실행하는 Python 환경 |
| clone | 원격 저장소를 내 PC로 복사 |
| commit | 변경 내용을 로컬 저장소에 기록 |
| push | 로컬 commit을 GitHub에 업로드 |
| display() | Jupyter Notebook 전용 출력 함수 |

---

## 3. 우선순위 요약

| 우선순위 | 항목 | 분류 |
|---------|------|------|
| 🔴 높음 | [1-1] git 설치 확인 없이 clone 사용 | 필수 수정 |
| 🔴 높음 | [1-3] Jupyter 커널 선택(.venv) 안내 없음 | 필수 수정 |
| 🔴 높음 | [1-6] .env.example → .env 생성 절차 없음 | 필수 수정 |
| 🔴 높음 | [1-12] commit/push 실습 절차 없음 | 필수 수정 |
| 🟠 중간 | [1-2] GitHub 계정 생성 안내 없음 | 필수 수정 |
| 🟠 중간 | [1-4] display() 함수 미설명 | 필수 수정 |
| 🟠 중간 | [1-5] requirements.txt 내용 확인 방법 없음 | 필수 수정 |
| 🟠 중간 | [1-7] 가상환경 비활성화 방법 미안내 | 필수 수정 |
| 🟠 중간 | [1-10] 경로 표기 혼재(PowerShell 섹션에서 /) | 필수 수정 |
| 🟠 중간 | [1-11] PowerShell 실행 정책 오류 - cmd 대안 없음 | 필수 수정 |
| 🟡 낮음 | [1-8] code . 명령 실패 원인 미설명 | 필수 수정 |
| 🟡 낮음 | [1-9] 수업 시간 합계 불일치 | 필수 수정 |
| 🟢 권장 | [2-1] GitHub 핵심 용어 설명 부재 | 보완 권장 |
| 🟢 권장 | [2-2] VS Code Python 확장 설치 안내 없음 | 보완 권장 |
| 🟢 권장 | [2-3] .gitignore 파일 역할 설명 없음 | 보완 권장 |
| 🟢 권장 | [2-5] 오류 발생 단계 안내 배치 개선 | 보완 권장 |
| 🟢 권장 | [2-8] 강사 저장소 URL 미명시 | 보완 권장 |
| 🟢 권장 | [2-10] 핵심 용어 정리 섹션 부재 | 보완 권장 |
| 🟢 참고 | [2-4] pip 최신화 이유 미설명 | 보완 권장 |
| 🟢 참고 | [2-6] Path import 중복 혼란 | 보완 권장 |
| 🟢 참고 | [2-7] 연습 문제 채점 기준 없음 | 보완 권장 |
| 🟢 참고 | [2-9] python vs python3 혼란 가능성 | 보완 권장 |

---

## 4. 전반적 평가

**잘 된 점**
- Windows/macOS 명령어를 별도 섹션으로 구분한 점이 명확함
- PowerShell 실행 정책 오류 대처법을 별도 섹션으로 제공한 점이 실용적임
- 강사 저장소 vs 개인 저장소의 역할을 표로 명확히 구분한 점이 우수함
- `FileNotFoundError` 상황을 가정한 디버깅 코드 제공(섹션 5.6)이 초보자에게 도움이 됨
- `.env` 파일 보안 원칙이 구체적으로 나열되어 있음
- 환경 관리 체크리스트(섹션 8)가 상세하고 실용적임

**전체적 방향 제안**  
2장은 비전공자가 "왜 이걸 해야 하는가"를 이해하면서 따라가야 하는 실습 장이다. 현재 구성은 절차 안내 위주로 잘 되어 있지만, **각 도구(git, 가상환경, Jupyter 커널)의 사전 조건 확인**이 빠져 있어 특정 단계에서 막히는 학생이 생길 수 있다. 특히 git 설치, GitHub 계정, Jupyter 커널 연결 문제는 수업에서 가장 자주 발생하는 장애물이므로 보강이 필수적이다.
