# ch01 강의안 검토 리포트

> **대상 독자**: 파이썬 기초 경험이 있는 비전공자, 데이터 분석 기본 개념 보유 학생  
> **검토 기준**: 한 학기 강의 교재로서 독립적 학습 가능 여부  
> **작성일**: 2026-06-19  
> **검토 파일**: `book/chapters/ch01_ai_data_analysis_intro.md`

---

## 검토 지침 (Codex Prompt Format)

아래 각 항목은 `[섹션명]` 위치를 기준으로 문제를 설명하고, 구체적인 수정/보완 방향을 제시합니다.  
**[필수 수정]** = 학습에 직접적 혼란을 야기하는 항목  
**[보완 권장]** = 추가 시 학습 효과가 크게 향상되는 항목

---

## 1. 필수 수정 항목

---

### [1-1] `pandas`와 `DataFrame` 개념 미설명 — [섹션 5.1]

**문제**  
`import pandas as pd` 코드 앞에 "데이터 분석에서는 표 형태의 데이터를 다룰 때 pandas를 자주 사용합니다"라고만 되어 있다. 비전공자 입장에서 pandas가 무엇인지, 왜 `pd`라는 짧은 이름을 쓰는지, DataFrame이 무엇인지 전혀 설명이 없다. 섹션 5.2에서 "pandas DataFrame이라는 표 형태의 객체"라는 표현이 나오지만, 이미 코드가 등장한 이후다.

**수정 지시**  
섹션 5.1 `import pandas as pd` 코드 앞에 다음 내용을 추가한다:

```
pandas는 파이썬에서 표 형태의 데이터를 다루는 라이브러리입니다.
엑셀의 시트처럼 행(row)과 열(column)로 구성된 데이터를 쉽게 읽고, 정리하고, 계산할 수 있습니다.
pandas로 읽어온 표 데이터를 DataFrame이라고 부릅니다.

`pd`는 pandas를 짧게 쓰기 위한 별칭(alias)입니다. 
`import pandas as pd`는 "pandas를 불러오되 앞으로 pd라는 이름으로 쓰겠다"는 의미입니다.
데이터 분석 커뮤니티에서 관례적으로 사용하는 표준 표기입니다.
```

---

### [1-2] `info()` 출력 예시 없음 — [섹션 5.5]

**문제**  
`shape`와 `head()`에는 예상 출력 예시가 제공되어 있지만, `info()`에는 실제 출력이 어떻게 생겼는지 보여주지 않는다. `info()` 결과에서 확인할 항목(컬럼 개수, 데이터 타입, 비어 있지 않은 값의 개수)을 설명하지만, 학생이 실제 출력을 보고 어느 부분이 그 항목인지 연결하기 어렵다.

**수정 지시**  
`customers.info()` 설명 다음에 예상 출력 예시를 추가한다:

```
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 150 entries, 0 to 149
Data columns (total 6 columns):
 #   Column       Non-Null Count  Dtype 
---  ------       --------------  ----- 
 0   customer_id  150 non-null    object
 1   name         150 non-null    object
 2   gender       150 non-null    object
 3   age          148 non-null    float64
 4   city         150 non-null    object
 5   signup_date  150 non-null    object
dtypes: float64(1), object(5)
memory usage: 7.2+ KB
```

출력 아래에 다음 설명을 추가한다:
- `Non-Null Count`가 전체 행 수(150)보다 작으면 그 컬럼에 결측치(빈 값)가 있다는 뜻입니다.
- `Dtype`이 `object`이면 문자열, `float64`나 `int64`이면 숫자입니다.
- `signup_date`가 `object`로 표시된다면 날짜 타입이 아닌 문자열로 저장된 것이므로, 날짜 분석 전에 변환이 필요합니다.

---

### [1-3] 샘플 데이터 생성 명령어 경로 표기 혼재 — [섹션 5]

**문제**  
`python scripts/generate_sample_data.py`는 리눅스/맥 경로 표기(슬래시 `/`)를 사용하고 있다. 이 교재가 Windows 환경을 기본으로 한다면 `python scripts\generate_sample_data.py`로 표기하거나, 두 운영체제 표기를 모두 안내해야 한다. 혹은 어느 환경에서든 동작하는 방식을 안내해야 한다.

**수정 지시**  
명령어 블록을 다음과 같이 수정한다:

```
# Windows 사용자
python scripts\generate_sample_data.py

# macOS / Linux 사용자
python scripts/generate_sample_data.py
```

또한 이 명령어를 어디서 실행해야 하는지(Jupyter Notebook의 터미널, VS Code 터미널, 또는 Anaconda Prompt) 명시한다.

---

### [1-4] 데이터 파일 관계(JOIN 개념) 설명 부재 — [섹션 4, 섹션 7.4]

**문제**  
"파일을 연결해야 한다"는 표현이 섹션 4와 7.4 모두에 등장하지만, 실제로 어떻게 연결하는지(pandas `merge()` 함수), 왜 기준 컬럼(key)이 필요한지에 대한 개념 설명이 전혀 없다. 비전공자는 "연결"의 의미를 직관적으로 이해하기 어렵다.

**수정 지시**  
섹션 4의 파일 관계 설명 아래에 간단한 비유와 개념 설명을 추가한다:

```
파일 연결 개념 이해하기

엑셀로 비유하면, 고객 목록 시트와 주문 내역 시트를 고객번호(customer_id)를 기준으로 합치는 것과 같습니다.
pandas에서는 merge() 함수를 사용해 두 파일을 연결합니다.
이 과정은 4장과 5장에서 자세히 다룹니다. 지금은 어떤 파일이 어떤 기준으로 연결되는지만 이해하면 됩니다.
```

---

### [1-5] `head()` 기본 행 수 미안내 — [섹션 5.4]

**문제**  
`head()` 함수가 "앞부분 몇 줄"을 보여준다고만 설명하고, 괄호 안에 숫자를 넣지 않으면 기본 5줄이 출력된다는 사실을 명시하지 않는다. `head(3)`, `head(10)` 같이 행 수를 조절할 수 있다는 것도 언급이 없다.

**수정 지시**  
`head()` 설명 문단에 다음 내용을 추가한다:

```
head()는 기본적으로 앞에서 5줄을 보여줍니다.
괄호 안에 숫자를 넣으면 원하는 줄 수만 볼 수 있습니다.

customers.head(3)   # 앞 3줄만 보기
customers.head(10)  # 앞 10줄만 보기
```

---

### [1-6] `head()` 4개 파일 코드가 설명 없이 나열만 됨 — [섹션 5.4]

**문제**  
`products.head()`, `orders.head()`, `order_items.head()`가 설명 없이 코드 블록으로만 나열되어 있다. 처음 학습하는 학생은 각 파일에서 무엇을 확인해야 하는지, 어떤 점이 다른지 알 수 없다.

**수정 지시**  
각 파일의 `head()` 코드 아래에 확인 포인트를 한 줄씩 추가한다:

```python
products.head()
# 확인 포인트: product_id가 어떤 형식인지, category에 어떤 값들이 있는지 확인합니다.

orders.head()
# 확인 포인트: order_date 형식, order_status에 어떤 상태 값이 있는지 확인합니다.

order_items.head()
# 확인 포인트: quantity와 unit_price가 숫자로 저장되어 있는지 확인합니다.
```

---

### [1-7] 학습 목표 7번 항목의 용어 설명 누락 — [섹션 1]

**문제**  
학습 목표 마지막 항목에 "프롬프트 로그와 데이터 보안이 중요한 이유"가 나오는데, "프롬프트 로그"는 1장 앞부분에서 처음 나오는 용어다. 학습 목표에서 처음 접하는 독자에게는 생소할 수 있다.

**수정 지시**  
학습 목표 항목을 다음과 같이 수정한다:

```
분석 과정에서 LLM 사용 기록(프롬프트 로그)과 데이터 보안이 중요한 이유를 이해할 수 있습니다.
```

또는 학습 목표 블록 아래에 한 줄 안내를 추가한다:
```
※ "프롬프트 로그"는 LLM에게 입력한 질문과 받은 답변을 기록해 두는 것을 의미합니다. 자세한 내용은 3.8절에서 다룹니다.
```

---

### [1-8] 섹션 3.2 흐름도 번호 목록과 본문 설명 불일치 — [섹션 3.2]

**문제**  
번호 목록에는 9단계(문제 정의 → 데이터 수집 → … → 의사결정 또는 후속 실험 제안)가 나오지만, 그다음 단락 "데이터 분석은 보통 문제 정의에서 시작해 … LLM 활용 보조 … 보고서 작성으로 이어집니다"라는 설명에서는 "LLM 활용 보조"가 별도 단계처럼 등장한다. 그런데 번호 목록에는 LLM 관련 단계가 없다. 두 설명이 서로 다른 흐름을 가리키는 것처럼 보여 혼란스럽다.

**수정 지시**  
번호 목록과 단락 설명을 일치시킨다. 방법은 두 가지 중 하나를 선택한다:

- 방법 A: 번호 목록의 5번(탐색적 데이터 분석) 또는 6번(시각화) 다음에 "LLM 보조 활용(선택)" 항목을 추가한다.
- 방법 B: 단락 설명에서 "LLM 활용 보조"를 별도 단계로 표현하지 않고, "탐색적 분석 단계에서 LLM을 보조적으로 활용할 수 있습니다"로 수정한다.

---

### [1-9] 섹션 4 분석 질문 변환 표: 컬럼 불일치 — [섹션 4]

**문제**  
"어떤 고객군이 많이 구매하는가?" 의 분석 질문 변환 예로 "성별, 연령대, 지역별 구매금액을 비교한다"가 제시된다. 그런데 `customers.csv`에는 `city` 컬럼이 있지, `region`(지역) 컬럼이 별도로 있지 않다. `city`는 도시명이며 지역 구분과는 다를 수 있다. 학생이 실제 데이터를 확인할 때 혼란을 야기한다.

**수정 지시**  
해당 표의 분석 질문 변환 예를 다음과 같이 수정한다:

```
성별, 연령대, 도시(city)별 구매금액을 비교한다.
※ 실제 데이터에는 지역(region) 컬럼이 없으므로 city 컬럼을 지역 대리 변수로 사용합니다.
```

---

### [1-10] 결과물 목록에 "LLM 사용 자기 평가"가 있지만 템플릿 없음 — [섹션 2, 섹션 8]

**문제**  
섹션 2의 결과물 목록 마지막에 "LLM 사용 자기 평가"가 있다. 그런데 섹션 8의 체크리스트는 제목이 "LLM 활용 자기 점검표"다. 두 이름이 다르고, 자기 평가 제출 형식이나 작성 방법에 대한 안내가 없다. 학생이 무엇을 제출해야 하는지 불명확하다.

**수정 지시**  
섹션 2의 결과물 목록을 수정한다:

```
- LLM 활용 자기 점검표 작성 (섹션 8의 체크리스트 활용)
```

또는 섹션 8 체크리스트 아래에 다음 안내를 추가한다:

```
이 점검표를 작성해 Jupyter Notebook 마지막 셀에 붙여 넣거나,
별도 Markdown 파일로 저장해 과제와 함께 제출합니다.
```

---

## 2. 보완 권장 항목

---

### [2-1] CSV 파일 개념 설명 추가 — [섹션 4 또는 섹션 5 앞]

**문제**  
"CSV 파일"이 처음부터 아무 설명 없이 등장한다. 비전공자 중 CSV가 무엇인지 모르는 학생이 있을 수 있다.

**보완 지시**  
섹션 4 또는 섹션 5 앞에 박스(Callout) 형식으로 한 문단 추가한다:

```
📌 CSV란?
CSV(Comma-Separated Values)는 데이터를 쉼표로 구분해 저장한 텍스트 파일입니다.
메모장으로 열면 쉼표로 나뉜 텍스트가 보이고, 엑셀로 열면 표 형태로 보입니다.
pandas의 read_csv() 함수로 불러올 수 있습니다.
```

---

### [2-2] `EDA`(탐색적 데이터 분석) 용어 설명 추가 — [섹션 3.2]

**문제**  
9단계 흐름에 "탐색적 데이터 분석"이 나오지만 이 용어의 의미와 영문 약어(EDA: Exploratory Data Analysis)가 설명되지 않는다. 이후 장(6장)에서 자세히 다루더라도, 1장에서 처음 접하는 학생을 위한 한 줄 설명이 필요하다.

**보완 지시**  
"탐색적 데이터 분석" 단계 옆에 한 줄 주석을 추가한다:

```
5. 탐색적 데이터 분석(EDA, Exploratory Data Analysis): 데이터를 다양한 각도에서 살펴보며 패턴과 이상을 발견하는 과정
```

---

### [2-3] `info()` 4개 파일이 각각 별도 블록으로 나뉘어 있어 실습 흐름 끊김 — [섹션 5.5]

**문제**  
`customers.info()`, `products.info()`, `orders.info()`, `order_items.info()`가 각각 별도의 코드 블록으로 나뉘어 있다. 실제 Jupyter Notebook에서는 하나의 셀에 묶어 실행하거나, 반복문으로 처리하는 것이 더 효율적이다. 초보자가 각각 실행해야 하는지, 한번에 실행해도 되는지 혼란스럽다.

**보완 지시**  
다음과 같은 통합 코드 예시를 추가하고, 개별 실행 코드는 보조 참고 형식으로 남긴다:

```python
# 4개 파일을 한 번에 확인하는 방법
for name, df in [("customers", customers), ("products", products),
                 ("orders", orders), ("order_items", order_items)]:
    print(f"\n===== {name} =====")
    print(df.info())
```

---

### [2-4] LLM 도구 선택 안내 부재 — [섹션 10 또는 섹션 3.3]

**문제**  
섹션 10 마지막에 "ChatGPT, Gemini, Claude 중 하나의 LLM 도구를 사용할 수 있는가?"라고 체크 항목이 있지만, 각 도구의 차이점이나 어떤 도구를 선택하면 좋은지에 대한 가이드가 전혀 없다. 처음 LLM을 접하는 학생은 어떤 도구를 선택해야 할지 모른다.

**보완 지시**  
섹션 3.3 또는 별도 박스로 간단한 도구 비교 표를 추가한다:

| 도구 | 접속 방법 | 무료 사용 | 특징 |
|------|-----------|-----------|------|
| ChatGPT | chat.openai.com | 제한적 무료(GPT-4o mini) | 가장 널리 쓰임, 코드 생성 우수 |
| Gemini | gemini.google.com | 무료 | 구글 계정으로 바로 사용 |
| Claude | claude.ai | 제한적 무료 | 긴 문서 처리, 설명이 상세함 |

```
이 교재의 프롬프트 예시는 어떤 도구에서도 동작합니다.
접근이 쉬운 도구부터 시작해도 됩니다.
```

---

### [2-5] 프롬프트 섹션에 역할(Role) 설정 이유 미설명 — [섹션 6]

**문제**  
섹션 6의 프롬프트 예시들이 "당신은 데이터 분석가입니다"로 시작하는 역할 설정(Role Prompting)을 사용하는데, 왜 이렇게 시작하는지 설명이 없다. 초보자는 이 문장이 왜 필요한지 이해하지 못한 채 무작정 따라 쓸 수 있다.

**보완 지시**  
섹션 6 도입부에 다음 내용을 추가한다:

```
프롬프트 작성 팁: 역할 설정(Role Prompting)

LLM에게 역할을 먼저 알려주면 더 전문적이고 일관된 답변을 얻을 수 있습니다.
"당신은 데이터 분석가입니다"와 같이 역할을 지정하면,
LLM이 일반적인 답변보다 데이터 분석 맥락에 맞는 답변을 생성하는 경향이 있습니다.
```

---

### [2-6] 연습 문제에 힌트 또는 채점 기준 없음 — [섹션 9]

**문제**  
연습 문제가 제출 형식과 조건만 있고, 힌트나 채점 기준이 없다. 비전공자가 혼자 학습할 경우 자신의 답변이 맞는지 확인할 방법이 없다.

**보완 지시**  
각 연습 문제 아래에 힌트를 추가하거나, 모범 답안 링크를 제공한다.  
최소한 다음 형식으로 채점 기준을 추가한다:

```
평가 기준:
- 분석 질문이 데이터로 측정 가능한 형태인가? (예: "매출이 좋다"보다 "월별 총매출이 전월 대비 증가했는가")
- 필요한 데이터 파일이 명시되어 있는가?
- 분석 방법 또는 사용할 pandas 함수가 언급되어 있는가?
```

---

### [2-7] 섹션 간 전환 문장 부재 — [전체 구조]

**문제**  
핵심 개념 섹션(3)에서 실습 시나리오(4)로, 실습 코드(5)에서 LLM 활용(6)으로 넘어갈 때 전환 문장이 없다. 학생이 "왜 갑자기 이 내용이 나오는가"를 이해하지 못할 수 있다.

**보완 지시**  
각 섹션 시작 또는 이전 섹션 끝에 1~2문장 전환 문장을 추가한다:

```
예시 (섹션 4 시작 전):
"앞에서 데이터 분석의 전체 흐름과 LLM의 역할을 이해했습니다.
이제 이번 수업 전체에서 사용할 실제 시나리오와 데이터를 살펴봅니다."

예시 (섹션 5 시작 전):
"데이터 파일이 무엇인지 파악했으니, 이제 직접 코드로 불러와서 확인해 봅니다."
```

---

### [2-8] 수업 시간 구성표와 섹션 구성 불일치 — [수업 시간 구성 표]

**문제**  
수업 시간 구성 표에는 "핵심 개념 이해(40분)", "실습 시나리오 이해(30분)", "데이터 구조 확인 실습(60분)", "LLM 프롬프트 실습(50분)", "결과 해석 및 정리(30분)"로 합계 3.5시간이다. 그런데 본문에는 "기본 수업은 약 3시간"이라고 적혀 있다. 30분 차이가 설명 없이 존재한다. 또한 어떤 섹션이 어떤 수업 시간에 해당하는지 매핑이 없다.

**보완 지시**  
수업 시간 구성 표를 다음과 같이 수정하고, 해당 섹션을 명시한다:

| 구성 | 권장 시간 | 해당 섹션 |
|------|-----------|-----------|
| 핵심 개념 이해 | 40분 | 섹션 3 |
| 실습 시나리오 이해 | 20분 | 섹션 4 |
| 데이터 구조 확인 실습 | 60분 | 섹션 5 |
| LLM 프롬프트 실습 | 40분 | 섹션 6 |
| 결과 해석 및 정리 | 20분 | 섹션 7, 8 |
| **소계** | **3시간** | |
| 연습 문제 (선택) | 60~90분 | 섹션 9 |

---

### [2-9] 데이터 파일 없을 때 오류 메시지 대처 방법 미안내 — [섹션 5]

**문제**  
`pd.read_csv("data/raw/customers.csv")`를 실행했을 때 파일이 없으면 `FileNotFoundError`가 발생한다. 초보자는 이 오류를 보고 당황할 수 있다. 샘플 데이터 생성 명령어(`python scripts/generate_sample_data.py`)가 먼저 실행되어야 한다는 안내가 있지만, 실패했을 때의 대처 방법이 없다.

**보완 지시**  
섹션 5 시작 전 또는 5.2 아래에 다음 내용을 추가한다:

```
⚠️ FileNotFoundError가 발생하면?

"No such file or directory: 'data/raw/customers.csv'" 오류가 나오면
샘플 데이터 파일이 아직 생성되지 않은 것입니다.
터미널에서 먼저 다음 명령어를 실행하세요:

python scripts/generate_sample_data.py

실행 후 data/raw/ 폴더에 customers.csv, products.csv, orders.csv, order_items.csv
4개 파일이 생성된 것을 확인하고 다시 실행합니다.
```

---

### [2-10] 데이터 관계도(그림 1-2)에 대한 텍스트 설명 보강 — [섹션 4]

**문제**  
그림 1-2(온라인 쇼핑몰 데이터 관계도)가 등장하지만, 그림을 보기 전 텍스트 설명이 다소 추상적이다. 처음 데이터베이스 관계 개념을 접하는 학생을 위해 더 직관적인 비유가 필요하다.

**보완 지시**  
그림 1-2 앞에 다음 비유 설명을 추가한다:

```
이 관계를 학교에 비유하면 이렇습니다:
- customers.csv = 학생 명단 (학번, 이름, 학과)
- orders.csv = 수강 신청 내역 (수강신청번호, 학번, 신청일)
- order_items.csv = 수강 과목 상세 (수강신청번호, 과목코드, 학점)
- products.csv = 과목 목록 (과목코드, 과목명, 분류)

"어떤 학생이 어떤 과목을 수강했는가?"를 알려면 네 시트를 학번과 과목코드로 연결해야 합니다.
```

---

### [2-11] 핵심 용어 정리 섹션 부재 — [전체 구조]

**문제**  
1장에 새롭게 등장하는 용어들(DataFrame, 결측치, 이상치, EDA, 프롬프트, 프롬프트 로그, merge, shape, head, info)이 각 섹션에 분산되어 있다. 비전공자가 학습 후 복습할 때 참고할 수 있는 용어 정리 섹션이 없다.

**보완 지시**  
섹션 10(정리) 또는 별도 부록으로 "이 장에서 사용한 주요 용어" 표를 추가한다:

| 용어 | 설명 |
|------|------|
| DataFrame | pandas에서 표 형태의 데이터를 저장하는 구조. 행과 열로 구성됨 |
| 결측치(Missing Value) | 데이터에서 값이 비어 있는 상태. NaN으로 표시됨 |
| EDA | Exploratory Data Analysis. 탐색적 데이터 분석 |
| 프롬프트 | LLM에게 입력하는 질문 또는 지시문 |
| 프롬프트 로그 | 분석에 사용한 프롬프트와 LLM 답변 기록 |
| shape | DataFrame의 (행 수, 열 수)를 반환하는 속성 |
| head() | DataFrame의 앞 n줄을 보여주는 함수 (기본 5줄) |
| info() | DataFrame의 컬럼 타입, 결측치 수를 요약해 보여주는 함수 |
| merge() | 두 DataFrame을 기준 컬럼으로 연결하는 함수 |

---

### [2-12] Jupyter Notebook 실행 방법 간략 안내 부재 — [섹션 5 앞]

**문제**  
"이번 장의 전체 실습은 `notebooks/ch01_ai_data_analysis_intro.ipynb`에서 실행합니다"라고 안내하지만, Jupyter Notebook을 처음 사용하는 학생을 위한 실행 방법이 없다. 그림 1-3(Jupyter Notebook 실행 화면)만으로는 어떻게 열고 실행하는지 알 수 없다.

**보완 지시**  
실습 안내 문단 아래에 다음 내용을 추가하거나, 2장(환경 설정)에서 자세히 다룬다는 전제로 간단한 참조 안내를 추가한다:

```
Jupyter Notebook 실행 방법은 2장에서 자세히 다룹니다.
이미 설치가 완료된 경우, 터미널에서 다음 명령어로 실행합니다:

jupyter notebook

그 다음 브라우저에서 notebooks/ch01_ai_data_analysis_intro.ipynb 파일을 엽니다.
```

---

## 3. 우선순위 요약

| 우선순위 | 항목 | 분류 |
|---------|------|------|
| 🔴 높음 | [1-1] pandas/DataFrame 개념 미설명 | 필수 수정 |
| 🔴 높음 | [1-2] info() 출력 예시 없음 | 필수 수정 |
| 🔴 높음 | [1-3] 경로 표기 혼재(Windows/Unix) | 필수 수정 |
| 🔴 높음 | [1-9] 분석 질문 변환 표 컬럼 불일치 | 필수 수정 |
| 🟠 중간 | [1-4] 파일 연결(JOIN) 개념 설명 부재 | 필수 수정 |
| 🟠 중간 | [1-5] head() 기본 행 수 미안내 | 필수 수정 |
| 🟠 중간 | [1-6] head() 4개 파일 확인 포인트 없음 | 필수 수정 |
| 🟠 중간 | [1-8] 섹션 3.2 흐름도 불일치 | 필수 수정 |
| 🟠 중간 | [1-10] 자기 평가 템플릿/제출 안내 없음 | 필수 수정 |
| 🟡 낮음 | [1-7] 학습 목표 용어 설명 누락 | 필수 수정 |
| 🟢 권장 | [2-1] CSV 개념 설명 추가 | 보완 권장 |
| 🟢 권장 | [2-2] EDA 용어 설명 추가 | 보완 권장 |
| 🟢 권장 | [2-4] LLM 도구 선택 안내 | 보완 권장 |
| 🟢 권장 | [2-5] 역할 설정 프롬프트 이유 설명 | 보완 권장 |
| 🟢 권장 | [2-8] 수업 시간표 섹션 매핑 | 보완 권장 |
| 🟢 권장 | [2-9] FileNotFoundError 대처 안내 | 보완 권장 |
| 🟢 권장 | [2-11] 핵심 용어 정리 섹션 추가 | 보완 권장 |
| 🟢 참고 | [2-3] info() 반복문 통합 코드 | 보완 권장 |
| 🟢 참고 | [2-6] 연습 문제 채점 기준 추가 | 보완 권장 |
| 🟢 참고 | [2-7] 섹션 간 전환 문장 추가 | 보완 권장 |
| 🟢 참고 | [2-10] 데이터 관계도 비유 추가 | 보완 권장 |
| 🟢 참고 | [2-12] Jupyter 실행 방법 안내 | 보완 권장 |

---

## 4. 전반적 평가

**잘 된 점**
- 전체 강의 흐름이 명확하게 제시되어 있고, 목표-결과물-정리 구조가 일관됨
- LLM의 한계와 사람의 검증 역할을 반복해서 강조한 점이 적절함
- 프롬프트 예시 5개가 구체적이고 실습에 바로 활용 가능함
- 개인정보 보안 섹션(3.9)이 1장에 포함된 것은 좋은 설계임
- LLM 활용 자기 점검표(섹션 8)가 체계적으로 구성됨

**전체적 방향 제안**
1장은 "개론"이지만 동시에 비전공자의 첫 진입점이다. 현재 내용 수준과 분량은 적절하나, **개념 설명의 깊이가 섹션마다 일관되지 않다** (일부는 너무 상세, 일부는 너무 간략). 특히 코드 실습 섹션(5)에서 출력 예시와 오류 대처가 보강되면 독립 학습 가능성이 크게 높아질 것이다.
