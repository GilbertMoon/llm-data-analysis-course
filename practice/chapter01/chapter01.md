# 1장 실습. AI와 함께하는 데이터 분석의 시작

> 이 문서는 학생이 순서대로 따라갈 수 있도록 작성한 실습 진행 가이드입니다.  
> Chapter 01에서는 복잡한 분석 코드를 만드는 것보다 **질문 정의 → AI 초안 → 실제 데이터와 비교 → 사람 검증** 흐름을 직접 경험하는 것이 목표입니다.

---

## 실습 목표

이 실습을 마치면 다음을 할 수 있습니다.

- 막연한 업무 질문을 분석 가능한 질문으로 바꿀 수 있습니다.
- LLM에게 분석 질문 후보를 요청할 수 있습니다.
- LLM이 제안한 컬럼과 분석 방법이 실제 데이터 구조와 맞는지 확인할 수 있습니다.
- AI가 제안한 내용을 그대로 사용하지 않고 실제 반영 여부를 판단할 수 있습니다.
- 간단한 프롬프트 로그를 남길 수 있습니다.
- 개인정보와 API Key를 LLM이나 Public GitHub에 올리지 않는 기본 원칙을 설명할 수 있습니다.
- Chapter 01 Notebook의 역할을 이해하고, 환경이 준비된 경우 기본 import 셀을 확인할 수 있습니다.

---

## 사용할 파일

이번 장에서 주로 확인할 파일은 다음과 같습니다.

- 실습 Notebook: `notebooks/ch01_ai_data_analysis_intro.ipynb`
- 고객 데이터: `data/raw/customers.csv`
- 상품 데이터: `data/raw/products.csv`
- 주문 데이터: `data/raw/orders.csv`
- 주문 상세 데이터: `data/raw/order_items.csv`
- 환경변수 예시: `.env.example`
- 저장소 안내: `README.md`

> 현재 Chapter 01 Notebook은 본격 분석용 완성 Notebook이라기보다 기본 import와 데이터 경로를 준비한 **starter scaffold**입니다.  
> Python·VS Code·가상환경·Jupyter 설정이 아직 끝나지 않았다면 Notebook 실행은 Chapter 02 이후에 진행해도 됩니다.

---

# STEP 1. 막연한 업무 질문을 분석 질문으로 바꾸기

## 1-1. 목적

데이터 분석은 코드보다 질문을 먼저 구체화해야 합니다.

다음 업무 질문을 사용합니다.

```text
요즘 매출이 줄어든 것 같은데 왜 그런가요?
```

이 질문은 그대로 분석하기에는 모호합니다.

## 1-2. 실행

아래 네 항목을 직접 적어 봅니다.

| 항목 | 내가 정할 내용 |
| --- | --- |
| 대상 | 무엇을 분석할 것인가? |
| 기준 | 어떤 단위로 볼 것인가? |
| 비교 | 무엇과 비교할 것인가? |
| 목적 | 결과를 어디에 사용할 것인가? |

예시:

```text
대상: completed 주문 기준 금액
기준: 월별
비교: 최근 6개월 월별 변화
목적: 감소가 특정 월 또는 특정 카테고리에 집중되는지 확인
```

이제 질문을 다음처럼 바꿔 봅니다.

```text
최근 6개월 동안 completed 주문 기준 월별 금액은 어떻게 변했으며,
감소가 특정 상품 카테고리에서 두드러지는가?
```

## 1-3. 예상 결과

처음 질문보다 다음이 명확해져야 합니다.

```text
기간
주문 상태 범위
집계 단위
비교 기준
분석 목적
```

## 성공 기준

- [ ] 막연한 질문 1개를 선택했습니다.
- [ ] 대상·기준·비교·목적을 적었습니다.
- [ ] 분석 가능한 문장으로 다시 작성했습니다.
- [ ] “매출”이라는 말을 썼다면 실제 계산 범위를 따로 정의했습니다.

## 오류가 나면

질문이 너무 크면 한 번에 모든 원인을 찾으려 하지 않습니다.

예:

```text
왜 매출이 줄었나요?
```

보다 다음처럼 나눕니다.

```text
월별 completed 주문 금액은 줄었는가?
주문 수가 줄었는가?
주문당 평균 금액이 줄었는가?
특정 카테고리에서 감소가 큰가?
```

---

# STEP 2. 실제 데이터 구조와 질문 연결하기

## 2-1. 목적

좋은 질문이라도 현재 데이터에 필요한 정보가 없으면 바로 분석할 수 없습니다.

이번 강좌의 기본 데이터는 네 파일입니다.

```text
customers.csv
products.csv
orders.csv
order_items.csv
```

## 2-2. 실행

다음 관계를 확인합니다.

```text
customers.customer_id
        ↓
orders.customer_id

orders.order_id
        ↓
order_items.order_id

products.product_id
        ↓
order_items.product_id
```

브라우저에서 다음 공개 이미지를 확인해도 됩니다.

```text
book/assets/images/ch01/ch01_shopping_data_relationship.png
```

질문에 어떤 파일이 필요한지 적어 봅니다.

예:

```text
질문: 카테고리별 completed 주문 금액을 비교하고 싶다.

필요 파일
- products.csv
- orders.csv
- order_items.csv

필요 컬럼 후보
- product_id
- category
- order_id
- order_status
- quantity
- unit_price
```

## 2-3. 예상 결과

질문과 필요한 데이터가 연결되어야 합니다.

## 성공 기준

- [ ] 고객·상품·주문·주문상세 네 파일의 역할을 설명할 수 있습니다.
- [ ] `customer_id`, `order_id`, `product_id` 관계를 이해했습니다.
- [ ] 내가 만든 질문에 필요한 파일을 선택했습니다.
- [ ] 필요한 컬럼 후보를 적었습니다.

## 오류가 나면

실제 CSV를 아직 열 수 없는 환경이라면 지금 단계에서는 파일 관계와 예상 컬럼만 확인합니다.

실제 컬럼과 데이터 타입 검증은 Chapter 03에서 다시 진행합니다.

---

# STEP 3. LLM에게 분석 질문 후보 요청하기

## 3-1. 목적

LLM을 정답 생성기가 아니라 **아이디어와 초안을 만드는 보조 도구**로 사용합니다.

## 3-2. 실행

ChatGPT, Gemini 등 사용 가능한 LLM에 다음과 비슷한 Prompt를 입력합니다.

```text
온라인 쇼핑몰 데이터 분석을 준비하고 있습니다.

데이터는 다음 4개 파일로 구성됩니다.
- customers: 고객 정보
- products: 상품 정보
- orders: 주문 정보
- order_items: 주문 상세 정보

목적은 completed 주문 기준 금액과 구매 패턴을 이해하는 것입니다.

초보 데이터 분석자가 먼저 확인할 분석 질문 5개를 제안해 주세요.
각 질문마다 필요한 데이터 파일과 확인할 컬럼 후보도 적어 주세요.
원인을 단정하지 말고, 현재 데이터로 확인 가능한 질문만 제안해 주세요.
```

> 실제 고객 행, 실제 이메일, 전화번호, API Key, 내부 URL 같은 민감정보는 입력하지 않습니다.

## 3-3. 예상 결과

다음과 비슷한 질문 후보가 나올 수 있습니다.

```text
월별 completed 주문 금액은 어떻게 변하는가?
카테고리별 completed 주문 금액 차이는 어떤가?
고객별 구매 금액 분포는 어떤가?
주문 수와 주문당 평균 금액은 어떻게 변하는가?
취소 주문 비율은 기간별로 어떤 차이가 있는가?
```

정확히 같은 답이 나올 필요는 없습니다.

## 성공 기준

- [ ] LLM에게 원본 개인정보 없이 질문했습니다.
- [ ] 분석 질문 후보를 받았습니다.
- [ ] 각 질문의 필요한 파일·컬럼 후보를 확인했습니다.
- [ ] LLM 답변을 아직 정답으로 확정하지 않았습니다.

## 오류가 나면

답변이 너무 일반적이면 Prompt에 범위를 더 넣습니다.

예:

```text
completed 주문만 금액 분석 범위로 사용하세요.
고객 개인정보 자체를 분석 목표로 사용하지 마세요.
원인 추정 대신 확인 가능한 지표 질문만 제안하세요.
```

---

# STEP 4. LLM 제안을 실제 데이터 기준으로 검증하기

## 4-1. 목적

LLM은 존재하지 않는 컬럼이나 사용할 수 없는 분석을 제안할 수 있습니다.

따라서 제안을 그대로 사용하지 않고 검증합니다.

## 4-2. 실행

LLM이 제안한 질문 중 1개를 선택하고 아래 표를 작성합니다.

| 검증 항목 | 확인 내용 |
| --- | --- |
| 질문 | 실제로 무엇을 묻는가? |
| 필요한 파일 | 저장소에 존재하는가? |
| 필요한 컬럼 | 실제 컬럼인지 확인이 필요한가? |
| 계산 범위 | completed/cancelled 등 범위가 정의되었는가? |
| 결과 해석 | 데이터에 없는 원인을 단정하고 있지 않은가? |
| 사용 여부 | 사용 / 수정 후 사용 / 보류 |

예:

```text
LLM 제안: 연령대별 구매 금액을 비교하세요.

검증
- customers.csv에 age가 있는지 확인 필요
- 주문 금액은 orders와 order_items를 연결해야 함
- completed 주문만 포함할지 기준 필요
- 연령이 구매 원인이라고 단정하면 안 됨

판단
= 수정 후 사용
```

## 4-3. 예상 결과

LLM 제안이 다음 셋 중 하나로 분류되어야 합니다.

```text
사용
수정 후 사용
보류
```

## 성공 기준

- [ ] LLM 제안 1개 이상을 검토했습니다.
- [ ] 실제 데이터가 필요한 부분을 표시했습니다.
- [ ] 계산 범위를 확인했습니다.
- [ ] 원인 단정이 있으면 제거했습니다.
- [ ] 최종 사용 여부를 사람이 결정했습니다.

## 오류가 나면

아직 실제 CSV를 확인하지 못했다면 “검증 완료”라고 쓰지 않습니다.

다음처럼 기록합니다.

```text
상태: 확인 필요
이유: 실제 컬럼 존재 여부를 Chapter 03에서 검증 예정
```

---

# STEP 5. 간단한 프롬프트 로그 남기기

## 5-1. 목적

AI가 분석에 어떤 영향을 주었는지 나중에 확인할 수 있도록 기록합니다.

## 5-2. 실행

아래 형식으로 메모를 남깁니다.

```markdown
## Chapter 01 Prompt Log

- 사용 목적:
- 입력 Prompt 요약:
- LLM 답변 요약:
- 실제 반영 여부:
- 사람이 검증한 항목:
- 남은 확인 사항:
```

예:

```text
사용 목적: 온라인 쇼핑몰 분석 질문 후보 만들기
입력 Prompt 요약: completed 주문 기준 분석 질문 5개 요청
LLM 답변 요약: 월별, 카테고리별, 고객별 질문 제안
실제 반영 여부: 월별·카테고리 질문만 반영
사람이 검증한 항목: 데이터 파일 관계, 주문 상태 범위
남은 확인 사항: 실제 컬럼과 데이터 타입은 Chapter 03에서 확인
```

## 5-3. 예상 결과

AI의 답과 사람의 판단이 구분되어 보여야 합니다.

## 성공 기준

- [ ] Prompt 사용 목적을 기록했습니다.
- [ ] LLM 답변 전체를 무조건 복사하지 않고 핵심만 요약했습니다.
- [ ] 실제 반영 여부를 기록했습니다.
- [ ] 사람이 검증한 내용을 기록했습니다.
- [ ] 개인정보나 Secret을 로그에 넣지 않았습니다.

---

# STEP 6. 개인정보와 Secret 보호 확인하기

## 6-1. 목적

LLM과 Public GitHub를 사용할 때 공개하면 안 되는 정보를 구분합니다.

## 6-2. 실행

다음 항목은 입력하거나 커밋하지 않습니다.

```text
실제 이름
이메일
전화번호
주소
결제 정보
DB 비밀번호
API Key
Client Secret
Access Token
회사 내부 URL
비공개 업무자료
```

API Key가 필요한 이후 Chapter에서는 `.env.example`을 참고해 로컬 `.env`를 사용합니다.

잘못된 예:

```python
API_KEY = "실제_API_KEY"
```

권장 방식:

```text
.env
+ .gitignore
+ 환경변수
```

## 6-3. 예상 결과

Public 저장소와 Notebook 출력에 실제 Secret이 없어야 합니다.

## 성공 기준

- [ ] 실제 개인정보를 LLM Prompt에 넣지 않았습니다.
- [ ] 실제 API Key를 Notebook에 적지 않았습니다.
- [ ] Prompt Log에도 민감정보가 없습니다.
- [ ] `.env`와 `.env.example`의 차이를 이해했습니다.

## 오류가 나면

실제 API Key를 원격 GitHub에 올렸다면 문자열만 지우고 끝내면 안 됩니다.

```text
키 폐기 또는 재발급
→ 현재 파일 제거
→ Git 기록 노출 여부 확인
→ 새 키는 안전한 방식으로 보관
```

---

# STEP 7. Chapter 01 Notebook 확인하기

## 7-1. 목적

현재 제공된 Chapter 01 Notebook의 역할을 정확히 이해합니다.

Notebook:

```text
notebooks/ch01_ai_data_analysis_intro.ipynb
```

현재 이 Notebook은 다음을 포함합니다.

```text
pandas
numpy
matplotlib
seaborn
Path
DATA_DIR = Path('../data/raw')
```

그리고 실제 CSV 로드는 예시 코드가 주석으로 제공되어 있습니다.

## 7-2. 실행

### 환경이 아직 준비되지 않았다면

Notebook 파일이 존재하는지만 확인하고 **Chapter 02를 먼저 진행**합니다.

### 환경이 이미 준비되어 있다면

VS Code에서 Notebook을 열고 import 셀을 실행합니다.

```python
from pathlib import Path

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

DATA_DIR = Path('../data/raw')
sns.set_theme(style='whitegrid')
```

그다음 데이터가 준비되어 있다면 다음 예시를 직접 실행해 볼 수 있습니다.

```python
customers = pd.read_csv(DATA_DIR / 'customers.csv')
customers.head()
```

## 7-3. 예상 결과

정상 환경이라면 import 오류가 없어야 합니다.

CSV를 읽었다면 표의 처음 몇 행이 표시됩니다.

## 성공 기준

환경 미설정 학생:

- [ ] Notebook 위치를 확인했습니다.
- [ ] 실제 환경 설정은 Chapter 02에서 진행한다는 것을 알고 있습니다.

환경 설정 완료 학생:

- [ ] Notebook이 열립니다.
- [ ] import 셀이 오류 없이 실행됩니다.
- [ ] `DATA_DIR`가 `../data/raw`를 가리키는 이유를 이해합니다.
- [ ] 데이터가 준비된 경우 `customers.csv`의 첫 행들을 확인했습니다.

## 오류가 나면

### `ModuleNotFoundError`

Chapter 02에서 가상환경과 `requirements.txt` 설치를 먼저 완료합니다.

### `FileNotFoundError`

Notebook이 `notebooks/` 폴더에 있으므로 상대경로 `../data/raw`가 프로젝트 루트의 `data/raw`를 가리키는지 확인합니다.

현재 작업 위치와 파일 위치를 함께 확인합니다.

---

# STEP 8. Chapter 01 완료 Evidence 만들기

## 8-1. 목적

단순히 “읽었다”가 아니라 무엇을 이해하고 검증했는지 남깁니다.

## 8-2. 실행

다음 항목을 한 문서 또는 메모에 정리합니다.

```text
1. 원래 업무 질문
2. 구체화한 분석 질문
3. 필요한 데이터 파일
4. LLM에게 사용한 Prompt 요약
5. LLM이 제안한 내용
6. 실제 사용 / 수정 / 보류 판단
7. 사람이 확인한 검증 항목
8. 남아 있는 확인 사항
```

## 8-3. 예상 결과

AI의 제안과 사람의 검증이 분리되어 보여야 합니다.

## 성공 기준

- [ ] 분석 질문을 직접 구체화했습니다.
- [ ] 필요한 데이터를 연결했습니다.
- [ ] LLM을 초안 도구로 사용했습니다.
- [ ] LLM 제안을 검증했습니다.
- [ ] Prompt Log를 남겼습니다.
- [ ] 민감정보를 사용하지 않았습니다.
- [ ] 미확인 사항을 PASS로 표시하지 않았습니다.

---

# 최종 검증

Chapter 01을 마치기 전에 다음을 확인합니다.

- [ ] 데이터 분석은 질문에서 시작한다는 것을 설명할 수 있습니다.
- [ ] 막연한 질문을 대상·기준·비교·목적으로 구체화할 수 있습니다.
- [ ] 가상 쇼핑몰 데이터 4종과 기본 관계를 설명할 수 있습니다.
- [ ] LLM이 분석 질문과 코드 초안을 도울 수 있다는 것을 이해했습니다.
- [ ] LLM이 실제 데이터와 업무 맥락을 자동으로 보장하지 않는다는 것을 이해했습니다.
- [ ] LLM 제안을 `사용 / 수정 후 사용 / 보류`로 판단했습니다.
- [ ] 프롬프트 로그를 남겼습니다.
- [ ] 실제 개인정보와 Secret을 사용하지 않았습니다.
- [ ] Notebook의 현재 역할을 starter scaffold로 이해했습니다.
- [ ] 환경이 미설정 상태라면 Chapter 02에서 설정을 먼저 진행할 계획입니다.

---

# 최종 산출물

학생이 남길 최소 Evidence는 다음입니다.

```text
Chapter 01 Analysis Question & AI Review

- 원래 업무 질문
- 구체화한 분석 질문
- 필요한 데이터
- LLM Prompt 요약
- LLM 답변 요약
- 실제 반영 여부
- 사람 검증 항목
- 남은 확인 사항
```

수업 운영자가 별도 제출 형식을 지정하지 않았다면 Markdown 메모, Notebook Markdown 셀 또는 학습 노트 형식으로 남겨도 됩니다.

---

# 다음 장 준비

다음은 **2장. VS Code에서 시작하는 데이터 분석 환경**입니다.

Chapter 02에서는 다음을 실제로 준비합니다.

```text
Python
→ VS Code
→ Git
→ 저장소 clone
→ .venv 생성
→ 패키지 설치
→ Jupyter Notebook
→ 커널 선택
→ 첫 실행 검증
```

Chapter 01에서 만든 분석 질문은 이후 데이터 구조와 pandas 실습을 진행하면서 실제 데이터로 검증하게 됩니다.